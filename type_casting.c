#include<stdio.h>
int main()
{
   double sum;
   int n = 0;
   sum = 0;
   while(n <= 10)
   {
        sum = sum + (1 / (double));
        printf("%2d %lf\n", n, sum);
        n++;
   }
   return 0;
}
